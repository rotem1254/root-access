You found it — this file lives inside the "projects" folder, and you walked into
that folder with `cd` to reach it.

Folders (also called directories) hold files and other folders, like drawers
inside drawers. `cd NAME` steps into one, `ls` shows what is inside, and
`cd ..` steps back out.

Your flag:

    FLAG{cd_1nt0_th3_f0ld3r}

Submit it:  submit FLAG{cd_1nt0_th3_f0ld3r}
